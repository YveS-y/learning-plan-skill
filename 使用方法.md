⏺ 整个流程分两步：

  ---
  第一步：初始化（一次性）

  在这个仓库（xuexi-learning-skill）里打开 Claude Code，调用 Skill：

  /learning-skill-v3

  然后向导会问你三个问题，你回答类似：

  1. ~/code/my-project（git项目路径）
  2. 大概看过README，没读过代码
  3. 能独立运用

  向导会：
  - 读项目结构（目录树、关键文件）
  - 设计关卡（L0 架构俯瞰 → L1 核心模块 → ...）
  - 在 ~/code/my-project/_learn/ 生成所有文件

  ---
  第二步：每次学习

  关闭这个窗口，重新打开 ~/code/my-project/_learn/ 作为工作目录：

  cd ~/code/my-project/_learn
  claude  # 或在 IDE 里打开这个目录

  Claude 自动读取 CLAUDE.md（陪练），然后：

  你：开始
  AI：[读课程卡，定位L0] Preview → Question
  你：[自己去读代码，然后回答]
  AI：[Feedback + 追问]
  ...通关 → L1 → ...

  ---
  一个实际例子

  假设你想学 credit-ops-agent/：

  _learn/ 位置 → credit-ops-agent/_learn/
  课程卡 L0   → 整体架构俯瞰（plan.md + README）
  课程卡 L1   → 学习旅程卡格式（learning-journey.md）
  课程卡 L2   → 错题本设计（kb-mistake-book.md）
  ...

  ---
  你现在有想学的项目吗？可以直接试一下，看看向导生成的课程卡质量怎么样。