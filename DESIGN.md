# 学习 Agent 系统设计文档

> 版本：v16 · 2026-05-23

---

## 1. 定位

将「我想学 X」变成跨话题、跨会话、持续生长的个人学习系统。核心产物是**知识库 + 判断力**，不是知识点记忆。

---

## 2. 学习理念

| 理念 | 在系统中的体现 |
|---|---|
| Bloom 掌握学习 | 不掌握不推进 |
| 费曼技巧 | 拒绝模糊描述，用自己的话说出来才算懂 |
| 苏格拉底提问 | 追问引导，不直接给答案 |
| Leitner 间隔重复 | 错题按盒子调度，防止遗忘 |
| 脚手架原则 | 新知识挂入已有知识框架 |

**理解证明的通用格式**：「做什么 · 为什么 · 边界在哪」（30-80字）

| 场景 | 做什么 | 为什么 | 边界在哪 |
|---|---|---|---|
| 代码节点 | 功能/约束 | 设计原因 | 取舍/不做什么 |
| 抽象概念 | 定义 | 存在的原因 | 失效的场景 |
| 操作技能 | 步骤 | 每步目的 | 容易出错处 |
| 框架/理论 | 核心假设 | 解决什么问题 | 适用边界 |

---

## 3. 角色

**向导开路 → 陪练磨炼 → 督导把关**

三个角色是职责划分，不是三个独立 AI。同一个 `_learn/CLAUDE.md` 按情境切换行为。

### 向导（Guide）
- **何时工作**：
  - ① Skill 初始化时：生成 `_learn/`；生成或更新学习计划表（已存在则追加新条目）
  - ② 阶段总结时：将本阶段学习成果归入知识库
  - ③ 临时追问后：即时建立或更新对应的知识库节点
  - ④ 错题毕业通知时：将该错题写为对应 KB 节点的来源例子，节点状态升为 ✅
- **职责**：分析输入 → 生成学习文件 → 对比知识库提议更新（规则见下）→ 接收错题毕业通知，将错题转为知识库节点的来源例子
- **知识库更新规则**：
  - 需要新建节点文件（无论是新领域、新大类还是新单节点）→ 展示 Mermaid 框架图 + 说明放置路径，需学员确认后建文件
  - 更新已有节点的状态或内容 → 直接操作，不中断对话
- **拆解方法论**：代码项目用「由宽到深 + 数据流法」；文档/课程用「问题驱动法 + 章节线性」；每关 ≤ 3 个新概念
- **重新拆解时机**：同一关卡连续 3 次答错 → 督导通知向导将该关拆成更小的子关卡

### 陪练（Sparring Partner）
- **何时工作**：每次学习会话中
- **深度学习**（项目/课程/文档）：5步循环 → Preview · Question · Explain · Feedback · 理解证明落库
- **碎片学习**：逐条过堂 → 一句话 + 举一个具体例子
- **通用规则**：模糊词不通过；学员说"明白了"先复述再判断
- **产出**：向督导汇报会话结果 + 错题写入 `_learn/错题本.md`

### 督导（Supervisor）
- **何时工作**：每次入会话 + 每关/条结束 + 触发阶段总结
- **入会话**：
  - 进度为空 → 从第一关（项目型）/ 第一条（碎片型）开始
  - 有进度 → 读进度快照（项目型来自课程卡.md；碎片型来自知识点清单.md），扫描到期错题，提示是否先复习
- **关卡结束（项目型）**：通关推进 / 未通关安排补课 / 同一关卡连续 3 次答错 → 通知向导拆更细
- **条目结束（碎片型）**：通过 → 勾选继续；未通过 → 写入错题本后继续下一条
- **阶段总结触发时机**：
  - 项目型：完成一个模块的所有关卡
  - 碎片型：知识点清单全部过堂完毕
  - 临时追问：不触发阶段总结，由向导即时处理
- **阶段总结动作**：汇总本阶段错题和新知识点 → 通知向导提议归入知识库

---

## 4. 数据层

### 两层结构

| 层次 | 内容 | 位置 | 生命周期 |
|---|---|---|---|
| **全局层** | 个人知识库、学习计划表 | `~/学习/`（用户指定，写入每个 CLAUDE.md）| 永久 |
| **项目层** | `_learn/` 全部文件 | 学习主题目录下 | 可删除，不影响项目代码 |

每个 `_learn/` 只服务一个学习主题，统一放到本地文件夹并上传 git：
- 代码项目：`_learn/` 放在项目根目录或子模块目录下
- 碎片型/文档型/AI生成课：在 `~/学习/` 下为该主题建一个独立文件夹，`_learn/` 放在里面

同一主题的补充内容（相关心得、新文章）新开独立 `_learn/`，在知识库阶段汇总融合，不混入原有 `_learn/`。

### 数据流向

```
临时概念追问
    → 向导即时建/更新知识库节点（新节点需学员确认）
    → 陪练进行问答
    → 答对：节点状态标 ✅
    → 答错：节点状态标 ⚠️（知识缺口），写入当前项目 _learn/错题本.md
           来源类型：深度学习（用「做什么·为什么·边界」标准复习，后续按 Leitner 调度）

碎片型（文章/逐字稿）
    → 生成 _learn/知识点清单.md → 陪练逐条过堂
        答错条目：即时写入 _learn/错题本.md，标记该条目（⚠️）
    → 过堂完毕：阶段总结，向导归入知识库
        答对条目：归入 KB 节点，状态标 ✅
        答错条目：归入 KB 节点，状态标 ⚠️（已在错题本中，后续按 Leitner 调度）

项目型（Git/文档/AI生成课）
    → 学习过程：错题写入 _learn/错题本.md
    → 模块完成：阶段总结，向导将知识点归入知识库
        答对条目：归入 KB 节点，状态标 ✅
        答错条目：归入 KB 节点，状态标 ⚠️（已在错题本中，后续按 Leitner 调度）
    → 项目通关：_learn/ 保留为档案，核心内容已归入知识库

错题毕业
    → 盒子5通过质量门槛
    → 督导通知向导
    → 向导将该错题写为对应知识库节点的「来源例子」
    → 若对应 KB 节点为 ⚠️，升为 ✅
```

### `_learn/` 文件夹

```
[项目或子模块目录]/
├── ... 项目原有文件（不动）
└── _learn/
    ├── CLAUDE.md        ← 角色行为定义 + 全局路径配置（每次必有）
    ├── 错题本.md        ← 项目级错题（每次必有）
    ├── 课程卡.md        ← 深度学习类型生成（Git/文档/AI生成课）
    ├── 知识点清单.md    ← 碎片型生成
    └── wiki.md          ← Git项目可选，初始化时向导询问是否生成；会话中若需引用但不存在则提示学员
```

**`_learn/CLAUDE.md` 必须包含**：
```
全局路径：
  知识库：~/学习/个人知识库/
  学习计划表：~/学习/学习计划表.md
```

### 个人知识库

节点状态以节点文件为权威，_index.md 和学习计划表从节点状态汇总，不单独维护：
- ✅ 已掌握：能回答「做什么·为什么·边界」，能处理反例
- ⚠️ 知识缺口：理解浅，或来自错题/临时追问
- ❓ 待学习：仅知道名字

```
~/学习/个人知识库/
├── AI工程/
│   ├── _index.md     ← Mermaid 结构图（汇总自节点状态）
│   ├── RAG/
│   └── Agent/
└── ...
```

**知识库节点文件结构**：
```markdown
# [知识点名]
状态：✅/⚠️/❓
做什么：        ┐
为什么：        ├ 向导在学员完成理解证明后填写
边界：          ┘
来源例子：
- [项目名/来源] 例子描述（错题毕业后由向导追加）
- [项目名/来源] 例子描述
```

### 错题本（项目层）

| 字段 | 说明 |
|---|---|
| ID、知识点、来源关卡 | 基本信息 |
| 题目描述 | 当时问了什么（≤30字，便于复习时精准定向）|
| 来源类型 | 深度学习 / 碎片型（临时追问归入深度学习类）|
| 能力标签 | 📝描述力 / 🔍判断力 / 📐参数直觉 / ⚖️取舍盲区 |
| 对应KB节点路径 | 如 `~/学习/个人知识库/AI工程/RAG/向量检索.md`（无则留空）|
| Leitner 盒子（1-5）| 1天/3天/7天/14天/30天 |
| 下次复习日期 | 绝对日期 |

**清除规则**：
- 深度学习类：能回答「做什么·为什么·边界」→ 升盒子
- 碎片类：一句话说清楚 + 举一个具体例子 → 升盒子
- 不通过 → 退回 max(1, 当前盒子-2)
- 盒子5通过 → 通知向导毕业转换

### 学习计划表（全局层）

向导初始化时生成，陪练和督导根据进度更新：

```markdown
# 学习计划表

## 活跃学习
- 项目：[_learn/路径]，当前进度：第[N]关（项目型）/ 第[N]条（碎片型），下次上课：[日期]

## 知识缺口（⚠️ 节点汇总）
<!-- 向导在阶段总结时扫描知识库节点状态，将所有 ⚠️ 节点同步至此列表 -->
- ⚠️ [知识点]（来源：[项目/追问]）

## 待学队列
- [ ] 主题/项目

## 已完成
- [x] [主题] ✅ [日期]
```

---

## 5. 角色切换机制

CLAUDE.md 定义固定的行为流程，不需要用户说特殊触发词。

**项目根目录 CLAUDE.md 引导语**负责处理 `_learn/` 不存在的情况：
```
用户说「我想学这个项目」或打开项目目录时：
    → 检查 _learn/ 是否存在
        不存在 → 「还没有学习文件，请运行 Skill 初始化」
        存在   → 「请将 _learn/ 目录作为工作目录打开 Claude Code 开始学习」
```

**`_learn/CLAUDE.md`** 只在用户以 `_learn/` 为工作目录打开 Claude Code 时生效：
```
以 _learn/ 为工作目录打开 Claude Code（自动读取 _learn/CLAUDE.md）
    → 督导判断类型：
        有课程卡.md       → 【项目型流程】
        有知识点清单.md   → 【碎片型流程】

【项目型流程】
    → 督导：
        进度为空           → 「从第一关开始」→ 进入陪练
        有进度             → 读进度快照（课程卡.md），扫到期错题
            有到期错题     → 「有N个错题到期，先复习？」
            无             → 等待
    ↓
收到「继续」/「开始」/「下一关」
    → 陪练：执行本节课
    ↓
关卡结束
    → 督导：
        通关             → 推进进度
        未通关           → 安排补课
        连续3次答错      → 通知向导拆更细，生成子关卡
    ↓
完成一个模块
    → 督导：触发阶段总结 → 通知向导归入知识库

【碎片型流程】
    → 督导：
        进度为空           → 「从第一条开始过堂」→ 进入陪练
        有进度             → 读知识点清单.md勾选状态，扫到期错题
            有到期错题     → 「有N个错题到期，先复习？」
            无             → 等待
    ↓
收到「继续」/「开始」/「下一条」
    → 陪练：逐条过堂
    ↓
一条完毕
    → 督导：
        通过   → 勾选该条目（✅），继续下一条
        未通过 → 写入 _learn/错题本.md，标记该条目（⚠️），继续下一条
    ↓
清单全部过堂完毕
    → 督导：触发阶段总结 → 通知向导归入知识库

【错题复习流程（入会话时学员确认后触发）】
    督导：按到期日期排序到期错题，逐条交给陪练
    ↓
    陪练：展示题目描述出题 → 学员回答
    ↓
    按来源类型判断：
        深度学习类：能回答「做什么·为什么·边界」→ 通过
        碎片类：一句话说清楚 + 举一个具体例子 → 通过
    ↓
    通过   → 升一格盒子，更新下次复习日期
               盒子5通过 → 督导通知向导毕业转换
    未通过 → 退回 max(1, 当前盒子-2)，更新下次复习日期
    ↓
    所有到期错题复习完毕 → 回到正常学习流程

【随时可中断插入，不依赖当前流程位置】
收到新概念追问
    → 陪练：负责全程问答交互（无论与当前关卡相关还是新知识点）
    → 向导（同时）：判断是否需要新建或更新 KB 节点 → 即时操作
    → 追问结束后，回到中断前的流程继续
```

---

## 6. 输入类型与方案

| 输入类型 | 触发场景 | 初始化输入 | _learn/ 核心文件 | 陪练方式 |
|---|---|---|---|---|
| **Git项目/子模块** | 读一个代码库 | 项目路径 | 课程卡.md（wiki.md 可选，初始化时确认）| 5步循环 |
| **结构化文档** | 书、PDF、技术文档 | 文档内容或路径 | 课程卡.md | 5步循环 |
| **AI生成课** | 无现成资料，从零学一个主题 | 主题 + 当前水平 + 学习目标 | 课程卡.md（向导设计）| 5步循环 |
| **碎片型** | 文章、逐字稿、心得笔记 | 本地文件路径或直接粘贴内容（不支持 URL，需先存为本地文件）| 知识点清单.md | 轻量过堂 |
| **临时追问** | 随时问一个新概念 | 概念名或问题 | 不生成文件 | 对话 |

所有方案的通过标准：深度学习用「做什么·为什么·边界」；碎片型用「一句话 + 举例」。

---

## 7. 设计边界

- 不一次性生成所有课程内容
- 不在未掌握时强行推进
- 不接受模糊描述（AI 判断，非自判）
- 不自动新建知识库节点文件，必须学员确认；已有节点的状态与内容更新直接操作
- 每个 `_learn/` 只服务一个学习主题，不混用
- `_learn/` 可删，不影响项目代码
- 详细文件模板在 `templates/` 中单独维护
